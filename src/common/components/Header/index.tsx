import React from 'react';

const Header = () => {
    return (
        <div className="app-header">
            <span className="app-header-brand">Shibaer</span>
        </div>
    );
};

export default Header;
